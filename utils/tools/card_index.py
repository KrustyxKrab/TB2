def card_index_generator():
    "Function to generate the column index of the loacto cards"
    while True:
        for i in range(4):
            yield i

    # this functions assigns a column value based to the cards

